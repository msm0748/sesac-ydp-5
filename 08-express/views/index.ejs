<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('./components/head', {title: '홈'}) %>
  </head>
  <body>
    <%- include("./components/header") %>
    <h1>Hello Express!</h1>
    <% const userId = 'apple'; %>
    <!-- server와 변수명이 겹치면 프론트 쪽 변수 우선 순위가 높아진다. -->
    <p>안녕하세요~~~~~<%= userId%>님! 환영합니다.</p>
    <p>비밀번호는 <%= userPw%> 입니다.</p>
    <% for (let i = 0; i < 5; i++) { %>
    <div><%= i%></div>
    <% } %>
    <h1><%= userId %></h1>
    <% for (let btn of btns) { %>
    <button type="button" class="btn btn-primary"><%= btn%></button>
    <% } %> <% if (!isLogin) { %>
    <h4>아직 로그인을 하지 않으셨군요!</h4>
    <% } else { %>
    <h4>로그인을 하셨군요</h4>
    <% } %>
  </body>
</html>
